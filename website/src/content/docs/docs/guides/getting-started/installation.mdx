---
title: Installation
description: Add the package to your project.
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { Icon } from '@astrojs/starlight/components'
import { LinkCard } from '@astrojs/starlight/components'

## Choose your framework

<Tabs syncKey="framework">
  <TabItem label="React" icon="seti:react">

    ## Install the package

    Choose your preferred package manager to install the library:

    <Tabs syncKey="pkg">
      <TabItem label="npm" icon="seti:npm">

        ```bash
        npm install @sankyu/react-circle-flags
        ```

      </TabItem>
      <TabItem label="pnpm" icon="pnpm">

        ```bash
        pnpm add @sankyu/react-circle-flags
        ```

      </TabItem>
      <TabItem label="yarn" icon="seti:yarn">

        ```bash
        yarn add @sankyu/react-circle-flags
        ```

      </TabItem>
      <TabItem label="bun" icon="bun">

        ```bash
        bun add @sankyu/react-circle-flags
        ```

      </TabItem>
    </Tabs>

    :::note[Peer Dependency]
    The package expects **React** to already be installed in your project.
    :::

  </TabItem>
  <TabItem label="Vue 3" icon="seti:vue">

    ## Install the package

    :::caution[Beta Release]
    `@sankyu/vue-circle-flags` is currently in **beta**. APIs may change in future releases.
    :::

    Choose your preferred package manager to install the library:

    <Tabs syncKey="pkg">
      <TabItem label="npm" icon="seti:npm">

        ```bash
        npm install @sankyu/vue-circle-flags
        ```

      </TabItem>
      <TabItem label="pnpm" icon="pnpm">

        ```bash
        pnpm add @sankyu/vue-circle-flags
        ```

      </TabItem>
      <TabItem label="yarn" icon="seti:yarn">

        ```bash
        yarn add @sankyu/vue-circle-flags
        ```

      </TabItem>
      <TabItem label="bun" icon="bun">

        ```bash
        bun add @sankyu/vue-circle-flags
        ```

      </TabItem>
    </Tabs>

    :::note[Peer Dependency]
    The package expects **Vue 3** to already be installed in your project.
    :::

  </TabItem>
  <TabItem label="Solid.js">

    ## Install the package

    :::caution[Beta Release]
    `@sankyu/solid-circle-flags` is currently in **beta**. APIs may change in future releases.
    :::

    Choose your preferred package manager to install the library:

    <Tabs syncKey="pkg">
      <TabItem label="npm" icon="seti:npm">

        ```bash
        npm install @sankyu/solid-circle-flags
        ```

      </TabItem>
      <TabItem label="pnpm" icon="pnpm">

        ```bash
        pnpm add @sankyu/solid-circle-flags
        ```

      </TabItem>
      <TabItem label="yarn" icon="seti:yarn">

        ```bash
        yarn add @sankyu/solid-circle-flags
        ```

      </TabItem>
      <TabItem label="bun" icon="bun">

        ```bash
        bun add @sankyu/solid-circle-flags
        ```

      </TabItem>
    </Tabs>

    :::note[Peer Dependency]
    The package expects **solid-js** to already be installed in your project.
    :::

  </TabItem>
</Tabs>

## Next step

<LinkCard
  title="Basic Usage"
  description="Learn how to import and render flag components in your application"
  href="../usage/"
/>
