{
  "name": "circle-flags-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for circle-flags UI components across multiple frameworks",
  "type": "module",
  "scripts": {
    "gen": "tsx scripts/gen.ts",
    "sync:examples": "tsx scripts/sync-example-shared.ts",
    "gen:flags": "tsx scripts/gen-flags.ts",
    "gen:svgs": "tsx scripts/gen-svgs.ts",
    "bump": "tsx scripts/bump-version.mjs",
    "bump:patch": "tsx scripts/bump-version.mjs --no-interactive --type patch",
    "bump:minor": "tsx scripts/bump-version.mjs --no-interactive --type minor",
    "bump:major": "tsx scripts/bump-version.mjs --no-interactive --type major",
    "bump:prerelease": "tsx scripts/bump-version.mjs --no-interactive --type prerelease",
    "bump:graduate": "tsx scripts/bump-version.mjs --no-interactive --type graduate",
    "bump:auto": "tsx scripts/bump-version.mjs --no-interactive --auto-detect --sync-deps",
    "postinstall": "pnpm run gen",
    "prebuild": "pnpm run gen:flags",
    "build": "pnpm run prebuild && pnpm -r -F './packages/*' run build",
    "build:core": "pnpm -F @sankyu/circle-flags-core run build",
    "build:react": "pnpm run prebuild && pnpm -F @sankyu/react-circle-flags run build",
    "build:vue": "pnpm run prebuild && pnpm -F @sankyu/vue-circle-flags run build",
    "build:solid": "pnpm run prebuild && pnpm -F @sankyu/solid-circle-flags run build",
    "dev": "pnpm -r --if-present run dev",
    "dev:package": "pnpm -r -F './packages/*' --if-present run dev",
    "dev:website": "pnpm -F './website' run dev",
    "dev:examples": "pnpm -r -F './examples/*' --if-present run dev",
    "typecheck": "pnpm run gen:flags && pnpm -r --parallel run typecheck",
    "lint": "pnpm -r --if-present run lint",
    "lint:fix": "pnpm -r --if-present run lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm -r --if-present --parallel run test",
    "test:package": "pnpm -r --if-present -F './packages/*' run test",
    "test:website": "pnpm -F './website/' run test",
    "test:examples": "pnpm -r --if-present -F './examples/*' run test",
    "test:core": "pnpm -F @sankyu/circle-flags-core run test",
    "test:react": "pnpm -F @sankyu/react-circle-flags run test",
    "test:vue": "pnpm -F @sankyu/vue-circle-flags run test",
    "test:solid": "pnpm -F @sankyu/solid-circle-flags run test",
    "test:coverage": "pnpm -F @sankyu/react-circle-flags run test:coverage && pnpm -F @sankyu/vue-circle-flags run test:coverage",
    "prepare": "husky",
    "clean": "rm -rf packages/*/dist packages/*/generated node_modules/.cache"
  },
  "author": "Sankyu Lab <opensource.sankyu-lab@xmsl.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SanKyu-Lab/circle-flags-ui.git"
  },
  "bugs": {
    "url": "https://github.com/SanKyu-Lab/circle-flags-ui/issues"
  },
  "homepage": "https://github.com/SanKyu-Lab/circle-flags-ui",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.0",
    "@commitlint/config-conventional": "^20.4.0",
    "@types/node": "^25.1.0",
    "commander": "^14.0.1",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "conventional-commits-parser": "^6.2.0",
    "country-region-data": "^4.0.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "semver": "^7.7.2",
    "simple-git": "^3.28.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
